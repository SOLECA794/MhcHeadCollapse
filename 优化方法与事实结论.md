# 优化方法与事实结论（MhcHeadCollapse 实战沉淀）

> **来源**：本轮优化实战（2026-09-11~13）发明/学到/验证的方法与结论。
> 实验原始数据见《实测结果汇总.md》，事实库见 FACTS.md（编号互引）。
> 最后更新：2026-09-13

---

## 一、发明的方法

### 1. 三级探针阶梯法（F2.1p 沉淀）

**用途**：把"性能瓶颈花在哪"从猜测变成逐项账本。

**做法**：在待测 kernel 上逐层掏空自有代码，每级一个 msprof 对照：
1. **掏计算**（FMA/标量运算换常量）
2. **掏访存**（UB GetValue/SetValue 换常量）
3. **掏同步**（删 SetFlag/WaitFlag 对）

每级 ~10 分钟（改→传→编→prof），级间差值即该层的真实贡献；
掏空后的残余即**框架地板**（不可削部分）。

**战果**：一次阶梯就测出 TinyH4 可削 scalar 仅 1.3μs、框架地板 ~3.9μs——
直接否决了"继续抠标量"的方向，把 ROI 转向结构级改造。

**纪律**：探针版 WA by design，**绝不提交平台**；用完存 versions/ 留档。

### 2. 先探针后实现（E-v117 教训）

静态指令计数 ≠ 运行时热点。v117 按静态分析投入完整实现（sigmoid 段 9→1 标量访问），
结果 scalar 持平——**1 轮编译-测试周期白费**。正确顺序：
探针（掏空待改段）预测收益上限 → 值得做才写完整实现 → 实现值应 ≈ 探针预测值
（v118 Row 路径兑现了预测，Group 路径反而暴露精度问题——预测-兑现对照两边都有信息量）。

### 3. dump 对账法（Group 精度 bug 诊断）

**用途**：kernel 内部值出错时，把中间量（gates）写到 y_gm_ 头部，
测试程序 D2H 拷回后与 python 逐值对账。

**要点**：
- dump 会被后续正常写回覆盖——dump 位置要选"最后写入"的通路，或对比被污染后的输出形态
- 用确定性输入（本项目测试是 sin/cos 公式，无随机）在 python 侧复现参考值
- 反解 gates（8×8 线性求解）在 fp16 输出下病态，只能看偏差形态不能看精确值

### 4. 频率热身曲线判读（E-S2）

同一二进制连续采样呈"双峰"时，先按调用序排时间序列——若是单调下降+稳态，
是**频率热身**（800→1650MHz）而非随机抖动。结论取稳态值；OJ 大概率也取稳态段
（榜首 4.52μs ≈ 本地稳态 4.4μs 互证）。

## 二、学到的方法

### 5. OJ 平台对账

- 排行榜 API（`/api/problems/{pid}/ranking`）公开全榜逐 case 时间——**先拉榜再定优化目标**
  （全榜最优 vs 自己的差距，比"感觉慢"可靠）
- 平台 `best_time` 字段是跨历史最优，比当前榜首还好

### 6. 向量化的数学等价性保持

- sigmoid 向量化：`-(lg*k+base) = (-lg)*k + (-base)`——取负折进常量，
  IEEE754 取负精确，**位级等价**
- 多项式 exp 向量化：Horner 展开逐操作复刻标量版（4 阶 Taylor on x/256 + 8 次平方），
  孤立验证误差 4e-11
- **但**：数学等价 ≠ 运行时等价——v118 Group 路径数学全对、四轮修复全不动误差，
  说明还有向量管线/布局层面的未知因素（根因未解，见 FACTS F2.1t）

### 7. 官方文档先行（ascend-kg 兑现）

写 AscendC 代码前先查昇腾知识图谱（ascend-kg skill）。
实例：检索直接命中官方文档标题"不要大量使用 SetValue 对 LocalTensor 进行赋值"，
v117 改造前就锁定了权威依据。

## 三、踩坑沉淀（硬规则）

### 8. OJ 提交三律（代价：榜上 0 分事故）

1. **排行榜只保留最新提交**——Compile Error 也覆盖在榜成绩。提交前必须确认比在榜分高
2. **远端增量编译会掩盖错误**——redefinition 级错误远端 BUILD_OK、平台报错。
   远端验证必须 clean build（rm -rf build）
3. **提交后必须轮询到终态 + 实时拉排行榜核验分数**——"提交成功"≠"榜上有分"

### 9. 远程操作纪律

- 命令保持单行（heredoc/多层引号会卡死远端 shell）
- 用绝对路径（`$HOME` 被本地 Git Bash 提前展开）
- 单核机器 `make -j2`（-j8 反而慢）
- /tmp 非持久，产物及时拉回
- 每命令前 `source set_env.sh`，否则找不到 aclnn 符号

### 10. 编译期跨类陷阱

成员变量声明的**类归属**要与使用处一致——v118 把 `base_pat_buf_` 声明在 TinyH4 类、
使用在 KernelMhcHeadCollapse 类，本地/远端增量编译都放过，只有平台全量编译抓到。

## 四、事实结论（决策依据）

| # | 结论 | 证据 | 影响 |
|---|---|---|---|
| F1 | 框架 scalar 地板 ~4μs，全局与路径/shape 无关 | 探针阶梯（F2.1p/q） | 结构级改造才有出路，抠标量上限 1.3μs |
| F2 | 真实带宽 109GB/s（非 200） | Case5 mte2 反推 | 一切按 109GB/s 的带宽估算 |
| F3 | Case1 的 mte2 1.72μs 是 DMA 启动延迟（288B 数据） | 0.17GB/s 荒谬值 | 小 shape 的 DMA 优化空间在合并/预取，不在带宽 |
| F4 | 频率热身：前 25 次调用慢 2~3 倍 | 1220 次时间序列 | 对比实验必须多采样取稳态 |
| F5 | streamed 口径含 ~4.4μs 主机底噪（单核 CPU） | F2.1m | 本地数字只做 A/B 相对比较 |
| F6 | 本地 910B4 与 OJ 同规格 | 三判据（F2.1h） | 本地测量可外推 |
| F7 | 榜首 Case5 4.52μs ≈ 4μs 地板 + 0.5 搬运 | F2.1q | 榜首已把 scalar 削到地板——我们差 3.2μs 中 ~1.5 在 gate 标量段（可削） |
| F8 | OJ 计时口径未确定（仅排除 per-call 同步/streamed/H2D 三个假设） | F2.1k | 不要用本地绝对值预测 OJ 分数 |
| F9 | 历史文档"Case3/4/5 shape 是推断"说法被推翻——n/nH 均为 v081 探针实测，仅 outer 未探 | F2.1l v2 | 断言"X 从未做过"前必须穷尽全部版本目录与补录段 |
| F10 | worker 产出的实验数据可能编造 | E-S2 事故 | 核心实验（进 FACTS 的数字）不外包；worker 数据必查远端产物 |

## 五、下一步方向（按 ROI，2026-09-13 时点）

1. **Group 路径向量 sigmoid 根因**：UB 级 dump（red 原始 ReduceSum 值）对账 phase 2 输出
   vs 向量链输入——一次定位错在哪一环。解开则 Case5 有望 8.5→~5μs
2. mte2 优化（PATH2 双峰驱动已排除=热身，但 mte2 本身 3.5μs 慢峰值的构成未拆）
3. Case3/4/5 的 outer 探测（OJ 探针提交，~10 分钟/轮）
