
修复完成并推送（ a8e6818 ）。交付报告：
▌ 做了什么

修好了一直阻塞 Case5 对拍的崩溃 bug（v119-fix，RED→GREEN 全程实测）。

先纠一个自己的误判：之前说“nH=1024 崩溃”是参数误读——测试程序参数是  <n> <h> <outer> ，崩溃场景实为 h=1024（即 nH=8192）。二分扫描锁定真实边界：nH=4096 PASS / nH≥4224 崩。

根因两级，靠“错误类型变化链”逐层剥出：第一级是 weight 全量缓存（n×nH×4，nH=4096 时 128KB）把逐行路径的 UB 总预算顶过 910B 单核可用上限（ascend-kg 确证：256KB 物理/192KB 可用）→ MTE 写越界。我的第一版修复（超限时 weight 改流式读单行）消掉了 MTE 错误，但暴露第二级： w_cache[i*nH_]  在流式缓冲（只有 nH 元素）上 i≥1 时索引越界 → VEC 读越界。补上索引修正后全绿。

验证：修复前稳定 507035 的 6 个 shape 全部 PASS（误差 6e-5~1e-4），10 个回归 shape 误差与修复前逐字一致（如 4/16/2 fp16 仍 8.848e-04）——group 路径、TinyH4、小 nH 零回归。在榜 5 case 不走此路径，分数零影响。

▌ 过程发现

这是 METHODOLOGY.md 观测基础设施的实战首秀且立功：UB 预算对账表（InitBuffer 逐项 vs 192KB）5 分钟锁定第一级根因，plog 错误串变化（MTE→VEC→PASS）精确定位第二级——比盲读 634 行 kernel 快得多。FACTS 已追加 F3.6 完整记录（含对 F3.3 误判的修正），STATUS 优先级列表更新。
▌ 下一步

按 STATUS：建 ACL-event 计时 harness（todo #2）——现在 shape 扫描矩阵没有任何阻塞了，全部 (n, nH, outer) 组合可测。

* 22m 25s · 69.71M→96.3k

❯ 可以可以，成果不错 可以继续迭代推进 记得维护系统

---

交付报告（commit  422956f ，已推送）：
▌ 做了什么

计时 harness 建成 + 首轮 shape 扫描矩阵到手——对拍核心件完成。 mhc_shape_bench.cpp （188 行）：ACL-event 双口径（percall 端到端 / streamed 均摊）、25 次热身剔除频率爬坡、med/min/p90 三统计量单行输出（脚本友好）。编译期踩了两个小坑已修：8.5 的  aclCreateTensor  是 9 参签名（strides+format+device）， aclrtEventElapsedTime  函数名。17 个 shape × 20 samples 的矩阵已存  shape_scan_v119_8.5.tsv  入 git。

首轮矩阵就有真发现（F3.7）：① outer=4 稳定慢 2~3μs——n=8 的两个 nH 档位上 outer=4 都比 outer=8 慢（11.96/13.05 vs 10.05/10.49μs）且 min≈med（稳定非抖动）。根因直指  block_dim  公式的临界缺陷： outer<=4 → 1 核 ，outer=4 时单核串行扛 4 行组路径。如果任一 OJ case 的 outer=4，这就是直接送分的优化点（一行公式改动）。② 本地 streamed 与 OJ 逐 case 时间仍有 2~4μs 系统偏移——口径问题再次实证，NOP 标定是从“有数据”到“可拟合”的必经一步。③ percall 口径在本机的 sync 开销 ~15μs（旧环境记录 4.4μs），本机 host 路径与旧环境显著不同。
▌ 下一步（todo #3/#4，按价值排序）

NOP 标定五连提交现在万事俱备（OJ 工具+凭据+可提交的 v119 代码）——它锁死口径系数后，17-shape 矩阵就能直接拟合 OJ 的 5 个时间，shape 之谜即解。outer=4 优化（todo #4）是一行改动，但建议等 shape 拟合确认 OJ case 真有 outer=4 再动（避免盲优化）。记得系统已维护：FACTS F3.7、STATUS、todo 全部同步，全部提交推送。

* 10m 58s · 75.99M→104.7k

❯ 可以，系统性结论维护进文件 继续推进剩余事项

---

