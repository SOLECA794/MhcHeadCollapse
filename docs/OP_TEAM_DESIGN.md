# 算子竞赛攻坚 Team v2 —— 以实测工作流反推的效率设计

> 设计原则：**从实测时间构成反推机制，而不是从职能头衔映射角色。**
> 目标：真实激发效率 = 消灭死等时间 + 根治重复事故，而非形式化的角色分工。

## 一、实测瓶颈（v080–v090 十个版本的真实时间构成）

| 时间去向 | 占比 | 本轮实例 |
| --- | --- | --- |
| 构建验证等待（本地 5–8min/次）+ OJ 提交等待（2–5min/次 + 429 限流） | ~55% | 每次 make/package/install 后主会话干等 |
| 事故处理 | ~25% | /tmp 清理丢 runner、scp 路径错、heredoc 转义、sigmoid 符号反、cst_buf 覆盖、z_vec 竞态 |
| 认知工作（假设设计、归因推理、模型修正） | ~20% | 计时模型 max(host节拍, kernel) 的推导、Case3/4/5 shape 反推 |

三个推论：
1. **最大杠杆是把死等时间流水线化**——构建/提交等待期间，应有其他上下文在工作。
2. **第二杠杆是把重复事故固化为脚本与清单**——事故几乎全是"同类第 N 次"。
3. **角色应按"上下文耦合度"划分，而非职能头衔**——设计与实现共享同一份 UB 布局心智模型，拆开反而产生转译损耗；而环境操作与记录是独立上下文，天然可并行。

## 二、4+1 结构（按上下文耦合度划分）

```
            ┌─────────────────────────────────────────────┐
            │  lead（主会话，唯一串行点）                    │
            │  假设排序 · 提交决策 · 验收归因 · 在榜风险管理   │
            └──────┬──────────────┬──────────────┬────────┘
                   │ 派发          │ 派发          │ 派发
      ┌────────────▼───┐  ┌───────▼────────┐  ┌──▼─────────────┐
      │ A kernel-dev    │  │ B env-runner    │  │ C oj-pilot      │
      │ 设计+实现+修编译  │  │ 910 环境          │  │ OJ 提交执行       │
      │ （同一心智模型，   │  │ 构建/安装/测试     │  │ 限流节奏/结果回读  │
      │  不拆 architect  │  │ 全部异步          │  │ 探针代发          │
      │  与 dev 两角色）  │  │                  │  │                  │
      └────────────────┘  └────────────────┘  └────────────────┘
                   ▲                ▲                  ▲
                   └────────────────┴──────────────────┘
                              D scribe（记录官）
                        实验落盘 · PITFALLS/FACTS/STATE 维护
                        （文件总线：所有人只读写共享文件，互不等待）
```

### A. kernel-dev（设计与实现合并——刻意不拆 architect/dev）
- **理由**：UB 布局、同步链、指令流在实现者脑中是同一份心智模型。拆成"设计师出文档→工程师转译"会引入转译损耗，本题实测单人设计+实现效率更高。
- **替代角色审阅的机制**：实现前强制跑一遍 **pre-flight 自检**（见三-3），这是清单闸门，不是另一个角色。
- **职责**：纸面推演（搬运量/指令数/同步配对/精度误差传播）→ 写码 → 本地编译通过 → 交给 env-runner。

### B. env-runner（本地 910 环境执行体，完全异步）
- **职责**：收到"验证 vXXX"指令后独立完成：上传 → 构建 → 打包 → 安装 → 跑测试矩阵 → 回写结果到 `STATE.md`。
- **固化要求**：构建-安装-测试是一条**原子远端脚本**（本轮最后的 base64+setsid 模式），杜绝 /tmp 清理、路径错、转义三类事故——脚本写对一次，永远复用。
- **判读权**：PASS/FAIL/误差值直接写入 STATE.md，lead 只读结论。

### C. oj-pilot（OJ 提交执行体，完全异步）
- **职责**：提交、限流等待与自动重试、结果回读（per-case Pass/RE/TLE + 计时）、探针代发、在榜状态监控。
- **固化要求**：提交+限流+重交+回读是一条脚本；429 时自动退避。
- **红线**：只执行 lead 的提交决策，永不自主提交（latest 计分，一次失误回滚成本极高）。

### D. scribe（记录官，唯一允许"迟到但不允许缺席"的角色）
- **职责**：每轮实验落盘（含证伪结论）；维护三份团队记忆文件。
- **铁律**：**证伪结论与成功结论同等入档**（v085/v087 的证伪各值一轮迭代）；版本目录随代码同步归档（v056 丢失教训）。

### lead（主会话本身，不设为子代理）
- 唯一串行点：假设排序、提交决策、验收归因、与用户对齐。
- lead 不写码、不跑构建——把 A/B/C/D 全部派出后，lead 的等待时间用于归因推理与下一假设设计。

## 三、激发效率的三板斧（机制，不是角色）

### 1. 流水线并行——消灭 55% 的死等
一轮标准迭代的并行时间线（目标：40–60 分钟压缩到 ~20 分钟）：

```
t0   lead: 定假设 v091 ──→ 派 A(实现) ──→ 派 C(重交 v090 试 Case1) ──→ 派 D(补录 v089/v090 记录)
t5   A: 代码完成 ──→ 派 B(构建+验证 v091)
t5–13 B 构建验证期间： lead 做归因/下一假设推演；C 回读 v090 结果；D 持续落盘
t13  B: PASS 证据就绪 ──→ lead 验收 → 派 C(提交 v091)
t13–18 C 提交期间： lead 依据 v091 结果预研 v092 假设
t18  结果回读 → scribe 落盘 → 下一轮
```

### 2. 操作固化——根治 25% 的事故
- env-runner 的构建链、oj-pilot 的提交链各固化为一个参数化脚本（版本号是唯一变量）。
- 每类事故首次出现即写入 PITFALLS.md 并**同步更新脚本/清单**，同坑不得二犯。
- 已固化清单（v090 时点）：文件级 pragma 必需、SetValue 替代 operator[] 赋值、向量 API 32B 对齐、事件 Set/Wait 严格配对不重 Wait、vexp 精度 1e-4、sigmoid=e^-z 方向、/tmp 原子脚本、base64 传参。

### 3. 清单闸门——用自检替代角色审阅
- **A 的 pre-flight（实现后提交前必过）**：①同步对逐条列出并核对配对；②所有 UB 偏移×count 与 InitBuffer 容量对账；③向量 API 基址 32B 对齐；④新数学的误差传播 ≤ 输出阈值；⑤文件级 pragma 在位；⑥多行/多批场景的复用变量清单。
- **B 的验证矩阵（固定）**：全部 5 个真实 case shape + fp32 抽查 + 边界 outer（1/4/8/16/100）。
- **C 的提交闸门**：lead 明确指令 + STATE.md 确认在榜状态 + 验证证据齐全。

## 四、什么必须串行（防过度并行）

1. **OJ 提交决策**——latest 计分，永远 lead 单点决策。
2. **kernel 演进主线**——v091 基于对 v090 结果的归因，不允许"赌两个版本同时上"。
3. **STATE.md 写入**——单一 scribe，避免并发写坏看板（其他角色只 append 事实，scribe 整理）。
4. **探针与正式版不混发**——探针会污染在榜分数，与止血提交严格分时。

## 五、落地（三层，按投入递增）

1. **零配置（本会话已可用）**：lead 用 Agent tool 并发 spawn 子代理，prompt 内联角色定义 + 必读文件路径（`迭代优化记录.md` 勘误小节、本文件），B/C 用 `run_in_background: true`。文件即总线，无需消息机制。
2. **半固化（推荐下一步）**：把 B/C 的两条远端脚本存为仓库文件（`scripts/verify.sh`、`scripts/submit.sh`），子代理 prompt 只需"执行 scripts/verify.sh v091 并把结论写回 STATE.md"。
3. **全固化**：项目级 subagent 定义文件（A/B/C/D 各一份），新会话零上下文即可开跑。

## 六、效率度量（让设计可被检验）

| 指标 | v080–v090 实测 | v2 目标 |
| --- | --- | --- |
| 单轮迭代周期（假设→平台结果） | 40–60 min | ≤ 20 min |
| 事故数/轮 | 2–3 | ≤ 0.5 |
| OJ 提交有效率（Pass 或产出信息） | ~70% | ≥ 90% |
| 记录滞后版本数 | 曾达 21 | ≤ 1 |

## 七、回溯验证（若 v2 早存在，本题可省多少）

- v087（WaitFlag 阻塞发射）：A 的 pre-flight ①条可直接拦截 → 省 1 轮（~40min）。
- v089 初版（sigmoid 符号反、operator[] 赋值 CE）：同上 → 省 1 轮。
- v059–v079 记录断层 + v056 源码丢失：D 铁律根治 → 省 20+ 个版本的重复探索。
- v081 探针 12 次提交（3 次设计缺陷白费）：C 的二分设计一次到位 → 省 3 次提交。
- 本轮多次"提交后才发现 Case1 TLE"：C 的在榜闸门 + 预判 → 减少被动回滚。
