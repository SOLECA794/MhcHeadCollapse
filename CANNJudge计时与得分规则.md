# CANNJudge 平台计时与得分规则

> **来源**：得分规则来自比赛附件（六、得分规则）；计时规则来自 CANNJudge 提交 Skill API 文档、社区算子实战记录及本项目迭代数据交叉验证。
>
> **整理时间**：2026-09-14
>
> **适用比赛**：中国电信星辰杯高校AI算子开发挑战赛（C组）—— MhcHeadCollapse 算子
>
> **⚠️ 重要单位说明**：平台 API 文档和提交脚本中打印的时间单位标注为"ms（毫秒）"，但经本项目实测验证，**实际单位为 μs（微秒）**。这是脚本打印单位标注错误，所有时间数值均应按微秒理解。本文档统一使用 μs。

---

## 一、概述

CANNJudge 平台对算子提交的评测分为两个维度：

1. **精度验证**：所有测试点的输出与参考实现对比，精度达标才算通过
2. **性能计分**：精度全部通过后，按每个测试点的执行时间计算性能分，最终取均值

核心计分公式为**对数衰减函数**，而非简单的线性比值——这对优化策略有直接影响（详见第七节）。

---

## 二、测试点与精度门槛

### 2.1 测试点数量

- 比赛共 **15 个测试点**（附件明确说明）
- 本项目迭代记录中提到的 5 个公开测试用例（ID 8213–8217，shape 均为 `[1, seq, 16]`，FP16）可能是 15 个测试点中的一部分，其余为隐藏测试点
- 隐藏测试点可能包含不同 shape（2D/3D）、不同 n 值（2/4/8）、不同 dtype（FP16/FP32/BF16）

### 2.2 精度门槛（全部通过才计分）

**所有测试点精度全部通过，才会计入性能分。** 任意一个测试点精度不达标，该提交不计性能分（排行榜上表现为无分数或 0 分）。

各 dtype 的精度容差（来自 CANNJudge 提交 Skill FAQ）：

| dtype | rtol（相对误差） | atol（绝对误差） |
|---|---|---|
| float16 | 1e-3 | 1e-3 |
| float32 | 1e-4 | 1e-4 |

> **注意**：输入含 NaN/Inf 时，对应位置输出应为 NaN（赛题边界条件要求），这也在精度验证范围内。

### 2.3 提交状态

| 状态 | 含义 | 是否计分 |
|---|---|---|
| Accepted | 全部测试点精度通过 | ✅ 计性能分 |
| Wrong Answer | 部分测试点精度不通过 | ❌ 不计分 |
| Compile Error | 编译失败 | ❌ 不计分 |
| Runtime Error | 运行时异常（如硬件 trap、越界） | ❌ 不计分 |
| Time Limit Exceeded | 超时 | ❌ 不计分 |

---

## 三、得分公式（核心）

### 3.1 公式

每个测试点单独计分，公式如下：

```
            100
得分 = ─────────────
        1 + log₁.₅(t/T)
```

其中：
- **T** = 最优性能（该测试点所有提交中的最短执行时间，即 best_time）
- **t** = 当前提交的性能（当前提交在该测试点的执行时间）
- **log₁.₅** = 以 1.5 为底的对数
- **时间单位**：μs（微秒，脚本打印标注为 ms 实为 μs）

### 3.2 参数说明

| 参数 | 含义 | 取值范围 |
|---|---|---|
| T | 该测试点历史最优时间（所有提交中的最小值） | T > 0 |
| t | 当前提交在该测试点的执行时间 | t ≥ T（因为 T 是最小值） |
| t/T | 当前性能与最优性能的比值 | ≥ 1.0 |

### 3.3 函数特性分析

**特性 1：t = T 时得满分**

当当前提交就是最优（t = T）：
```
得分 = 100 / (1 + log₁.₅(1)) = 100 / (1 + 0) = 100
```

**特性 2：对数衰减，边际收益递减**

得分随 t/T 增大而下降，但下降速度逐渐放缓：
- t/T 从 1.0 → 1.5（慢 50%）：得分从 100 → 50（降 50 分）
- t/T 从 1.5 → 2.25（再慢 50%）：得分从 50 → 33.3（降 16.7 分）
- t/T 从 2.25 → 3.375（再慢 50%）：得分从 33.3 → 25（降 8.3 分）

**特性 3：接近最优时，每微秒都价值巨大**

在 t/T 接近 1 的区间，时间的微小变化会导致得分的大幅变化。这意味着：
- 当你已经接近最优时，再抠 0.5μs 可能提升 10+ 分
- 当你远离最优时，抠 2μs 可能只提升几分

**特性 4：T 是动态刷新的**

T 是该测试点所有提交中的最短时间。每当有新的更优提交出现，T 就会被刷新，所有其他提交的得分会相应下降（因为 t/T 变大了）。这意味着排行榜分数是动态变化的，不是固定的。

### 3.4 典型值对照表

| t/T（慢于最优的倍数） | log₁.₅(t/T) | 得分 | 说明 |
|---|---|---|---|
| 1.0 | 0.000 | **100.00** | 平最优，满分 |
| 1.1 | 0.234 | 80.97 | 慢 10% |
| 1.2 | 0.450 | 68.98 | 慢 20% |
| 1.3 | 0.647 | 60.71 | 慢 30% |
| 1.5 | 1.000 | **50.00** | 慢 50%，得分腰斩 |
| 1.8 | 1.449 | 40.82 | 慢 80% |
| 2.0 | 1.710 | 36.91 | 慢 1 倍 |
| 2.25 | 2.000 | 33.33 | 慢 1.25 倍 |
| 2.5 | 2.259 | 30.68 | 慢 1.5 倍 |
| 3.0 | 2.710 | 26.96 | 慢 2 倍 |
| 5.0 | 3.969 | 20.12 | 慢 4 倍 |
| 10.0 | 5.678 | 14.97 | 慢 9 倍 |

> **关键观察**：t/T = 1.5 是一个重要分水岭——慢 50% 就只剩 50 分。t/T > 2 之后，得分下降明显放缓（从 36.9 到 27.0 只降了约 10 分，但时间又慢了 50%）。

---

## 四、计时规则

### 4.1 测量方式：端到端单算子 API 调用时间

平台使用单算子 API（aclopExecuteV2 系列）调用提交的算子，在 launch 前后通过 ACL event 机制打点计时。返回的 `time` 字段是**端到端执行时间（微秒 μs）**，不是 kernel 内部纯计算时间。

> **⚠️ 单位修正**：API 文档和提交脚本中标注为"毫秒（ms）"，但经本项目实测验证，实际单位为微秒（μs）。例如 v031 kernel 内部实测 4.18μs，OJ 返回 Case1 = 4.48，两者仅差 0.30μs（约 7%），若按毫秒理解则相差 1000 倍，与实际不符。

**正确计时的关键点**（来自 CANN 算子性能测试实战）：
- 算子下发是异步的，host 侧函数返回只是把任务扔到 stream 上
- 必须在计时前后同步 stream 或使用 ACL event，否则测到的只是 launch 延迟，不是实际执行时间
- 平台已经处理了同步，返回的 time 是包含同步的完整端到端时间

### 4.2 时间构成

端到端时间由以下部分组成：

| 组成部分 | 说明 | 占比（小 shape 场景，本项目实测） |
|---|---|---|
| Host 侧 Tiling 计算 | TilingFunc 执行，计算切分参数 | 小 |
| Kernel launch 调度 | 任务下发、核函数实例化、参数传递 | **~5–10%** |
| 数据搬运 GM→UB | MTE2 DMA 搬运输入数据 | 小（数据量小时） |
| 实际计算 | Vector/Scalar 单元执行 | **~90%+（主导）** |
| 结果写回 UB→GM | MTE3 DMA 搬运输出 | 小 |
| 同步等待 | stream 同步、event 等待 | 含在 launch 内 |

**关键证据（本项目实测）**：
- v031 kernel 内部实测执行时间：**4.18 μs**
- OJ 平台返回 Case1 时间：**4.48 μs**（脚本打印标注为 4.48ms，实为 μs）
- 差距仅 **0.30 μs（约 7%）**，证明 OJ 测的时间主要就是 kernel 执行时间，launch 开销只占很小比例
- 这与"毫秒"标注矛盾：若真为毫秒，则差距为 4480μs - 4.18μs ≈ 4476μs，launch 开销占 99.9%，与小 shape 场景的实际硬件行为不符

### 4.3 API 返回字段

每个测试点的结果包含以下字段（来自 CANNJudge 提交 Skill API 文档）：

| 字段 | 类型 | 含义 |
|---|---|---|
| `testcase_status` | string | Pass / Wrong Answer / Runtime Error / Time Limit Exceeded |
| `precision_ratio` | float | 精度比例（1.0 表示完全匹配） |
| `time` | float | 执行时间（微秒 μs，脚本标注为 ms 实为 μs），即公式中的 t |
| `best_time` | float | 该测试点历史最优时间（所有提交中的最小值），即公式中的 T |
| 比值 | float | t / best_time，即 t/T |

> `best_time` 和比值字段的存在，直接印证了计分公式中 T 和 t/T 的定义。

### 4.4 时间单位与精度

- **时间单位：微秒（μs）**（脚本和 API 文档中标注为"毫秒 ms"，实为标注错误）
- 平台返回的时间精度通常到小数点后 2 位
- 小 shape 场景下，时间主要由 kernel 实际执行决定，launch 开销只占 5–10%
- **因此 kernel 内部优化（向量指令、同步、快速数学、UB 布局）直接反映在 OJ 时间上，每 0.1μs 的提升都有意义**

---

## 五、排名规则

### 5.1 最终分数计算

```
最终分数 = 所有测试点得分的均值
```

即：
```
最终分数 = (Σ 每个测试点得分) / 测试点总数（15）
```

### 5.2 同分排序

若两个提交的最终分数计算一致，则按以下规则排序：

> **以提交时间进行排序，提交越早，排名越高。**

这意味着：
- 在分数相同的情况下，先提交的排名更靠前
- 后提交的同分数版本不会超越先提交的版本
- 这对 `ranking_submission_mode=latest`（取最新提交计分）的模式有影响：如果最新提交的分数与之前相同，排名可能反而下降（因为提交时间更晚）

### 5.3 计分提交模式

- `ranking_submission_mode = latest`：排行榜取**最新提交**的分数计分，不是最高分
- 这意味着每次提交都会覆盖排行榜上的分数，即使新版本更差
- 提交策略需要谨慎：确认新版本更优后再提交，避免拉低排名

---

## 六、实际数据示例

### 6.1 用本项目 Case1–5 数据验证公式

以下数据来自迭代优化记录（v093，截至 2026-09-03 排行榜第 4 名 lix123 的时间作为 T，实际冠军 T 可能更小。所有时间单位均为 μs）：

| 测试点 | T（参考最优，μs） | t（你们 v093，μs） | t/T | 得分 |
|---|---|---|---|---|
| Case1 | 2.94 | 4.48 | 1.524 | 49.05 |
| Case2 | 2.44 | 5.32 | 2.180 | 34.22 |
| Case3 | 3.24 | 4.38 | 1.352 | 57.35 |
| Case4 | 3.30 | 4.18 | 1.267 | 63.17 |
| Case5 | 5.46 | 7.98 | 1.462 | 51.65 |
| **5 点均值** | — | — | — | **51.09** |

> **注意**：
> 1. 此处 T 用的是排行榜第 4 名 lix123 的时间，实际冠军的 best_time 更小，因此实际得分会低于上表计算值
> 2. 比赛共 15 个测试点，此处只算了 5 个公开测试点，其余 10 个隐藏测试点的表现未知
> 3. 你们 v093 实际排行榜分数为 41.53，低于 5 点均值 51.09，原因可能是：冠军 T 更小 + 隐藏测试点表现更差 + 后续 T 被刷新
> 4. 所有时间单位均为 μs（微秒），脚本打印标注为 ms 实为标注错误

### 6.2 优化收益估算示例

以 Case2（当前最弱项，t/T = 2.18，得分 34.22）为例，估算不同优化程度的得分提升：

| 优化后 t（μs） | t/T | 得分 | 得分提升 |
|---|---|---|---|
| 5.32（当前） | 2.180 | 34.22 | — |
| 4.50 | 1.844 | 40.02 | +5.80 |
| 4.00 | 1.639 | 45.05 | +10.83 |
| 3.50 | 1.434 | 52.42 | +18.20 |
| 3.00 | 1.230 | 64.10 | +29.88 |
| 2.44（平最优） | 1.000 | 100.00 | +65.78 |

> Case2 是拉低总分的最主要因素。如果能将 Case2 从 5.32μs 降到 3.5μs，仅这一个测试点就能提升约 18 分，对 15 点均值的贡献约 +1.2 分。
>
> 由于 OJ 时间主要就是 kernel 执行时间（launch 仅占 5–10%），这些优化目标都可以通过 kernel 内部优化实现（向量指令调度、同步精简、快速数学、UB 布局优化等）。

---

## 七、对优化策略的影响

理解了对数计分公式和端到端计时规则后，优化优先级和策略需要相应调整：

### 7.1 优化优先级排序

| 优先级 | 优化方向 | 对 OJ 时间的影响 | 对得分的影响 |
|---|---|---|---|
| **最高** | kernel 内部指令级优化（向量/标量/同步/快速数学/UB 布局） | 直接减少 kernel 执行时间（占 OJ 时间 90%+） | 大（所有测试点通用） |
| **高** | 优化最弱测试点（如 Case2，t/T>2） | 降低该点 t/T | 大（对数计分，弱项提升收益高） |
| **高** | 精简 TilingKey（减少 kernel 实例化数量） | 减少 launch 开销（占 5–10%） | 中 |
| **中** | 压缩 TilingData 大小 | 减少 Host→核内拷贝开销 | 中 |
| **中** | 单 kernel 融合（减少 launch 次数） | 消除额外 launch | 中 |
| **中** | OpDef/dtype 组合精简 | 减少编译和调度开销 | 小-中 |

### 7.2 对数计分的策略含义

**含义 1：弱项优化的边际收益更高**

对数函数在 t/T 较大时斜率较小，但在 t/T 接近 1 时斜率很大。这意味着：
- 对于已经接近最优的测试点，再抠一点时间收益很大（但也很难）
- 对于远离最优的测试点（如 Case2，t/T=2.18），大幅优化的绝对得分提升也很可观
- **策略**：优先优化最弱的测试点，把 t/T 从 2+ 降到 1.5 以下，比在强项上从 1.3 抠到 1.2 收益更大

**含义 2：不要忽视任何一个测试点**

最终分数是 15 个测试点的均值。一个测试点崩了（如 TLE 或 WA），整个提交不计分。即使精度都通过，一个特别慢的测试点（t/T=10，得分 15）也会显著拉低均值。

**含义 3：T 是动态的，分数会被"卷"下去**

每当有人刷新某个测试点的 best_time，所有其他人在该点的 t/T 都会变大，得分下降。这意味着：
- 现在的 50 分，未来可能变成 45 分（如果 T 被刷新）
- 持续优化是必要的，不能满足于当前分数
- 但也要注意：越接近最优，提升难度越大，边际收益越高但越难实现

**含义 4：kernel 执行时间是真正的瓶颈，launch 开销只占小头**

由于端到端时间 90%+ 是 kernel 实际执行时间（本项目实测：v031 kernel 4.18μs vs OJ 4.48μs，差距仅 0.30μs），kernel 内部优化的边际收益是直接的、显著的。

**真正的突破口在 kernel 内部**：
- 向量指令调度（减少向量队列等待、提升向量单元利用率）
- 同步精简（减少 SetFlag/WaitFlag 次数，v031 已做到全 kernel 仅 1 次 V_S 同步）
- 快速数学（Newton rsqrt、Horner exp、多项式 sigmoid，平衡精度和延迟）
- UB 布局优化（减少 bank 冲突、提升数据局部性）
- 标量展开（减少循环开销，如 v031 的 16 元素突发加载）
- 数据搬运优化（DMA 预载、双缓冲、Loop 模式）

Host 侧 launch 开销优化（TilingKey、TilingData）仍有意义，但只影响 5–10% 的时间，不是主要瓶颈。

---

## 八、注意事项与不确定项

### 8.1 已确认

| 项目 | 状态 | 来源 |
|---|---|---|
| 得分公式 100/(1+log₁.₅(t/T)) | ✅ 确认 | 比赛附件 |
| 15 个测试点 | ✅ 确认 | 比赛附件 |
| 全部精度通过才计分 | ✅ 确认 | 比赛附件 |
| 最终分数为所有 case 均值 | ✅ 确认 | 比赛附件 |
| 同分按提交时间早的靠前 | ✅ 确认 | 比赛附件 |
| 计时为端到端时间 | ✅ 高置信 | API 文档 + 实测数据交叉验证 |
| **时间实际单位为 μs（非 ms）** | ✅ 确认 | 本项目实测：kernel 4.18μs vs OJ 4.48，差距仅 7% |
| best_time = T | ✅ 高置信 | API 返回字段 + 公式定义吻合 |

### 8.2 不确定 / 需进一步确认

| 项目 | 状态 | 说明 |
|---|---|---|
| 15 个测试点的具体构成 | ❓ 未知 | 已知 5 个公开测试点 shape，其余 10 个隐藏测试点的 shape/dtype/n 值未知 |
| 每个测试点的权重 | ❓ 未知 | 附件说"所有 case 得分的均值"，暗示等权，但未明确排除加权可能 |
| T 的刷新时机 | ❓ 未知 | 是实时刷新还是定时刷新？新最优提交后多久反映到其他人的分数里？ |
| 精度验证的参考实现 | ❓ 未知 | 是 NumPy 参考还是 PyTorch 参考？精度比较的具体方式（逐元素 vs 整体） |
| 超时阈值 | ❓ 未知 | 每个测试点的 Time Limit Exceeded 阈值是多少？ |
| score_mode=0 的精确含义 | ❓ 部分已知 | 迭代记录标注为"按性能加权"，与附件公式吻合，但 mode=0 是否对应此公式需确认 |

### 8.3 反推精确规则的实验方案

如需 100% 确认计分公式的细节（如权重、T 刷新机制），可设计以下实验：

1. **确认公式**：用一个已知正确的算子，提交一个故意加 sleep 的版本（拖慢时间），对比返回的 Score 和按公式计算的理论值
2. **确认权重**：如果各测试点等权，优化一个测试点对总分的影响应为 Δscore/15；如果不等权，可通过多次实验反推权重
3. **确认 T 刷新**：提交一个刷新某测试点 best_time 的版本，观察其他提交的分数是否立即变化
4. **确认隐藏测试点**：用不同 shape/dtype 的输入提交，观察哪些组合会导致 WA（说明隐藏测试点覆盖了这些组合）
5. **确认时间单位**：在 kernel 中插入已知延迟的空循环（如 NOP 循环），观察 OJ 返回时间的增量是否与 μs 一致

---

## 九、终极极简总结

> **计分公式**：每个测试点得分 = 100 / (1 + log₁.₅(t/T))，其中 T 是该点历史最优时间，t 是当前时间。15 个测试点精度全过后取均值，同分按提交时间早的靠前。
>
> **计时本质**：OJ 测的是端到端时间，**实际单位为 μs（微秒），脚本标注的 ms 是打印错误**。本项目实测验证：v031 kernel 内部 4.18μs，OJ 返回 4.48，差距仅 0.30μs（约 7%），证明 kernel 执行时间占绝对主导，launch 开销只占 5–10%。
>
> **优化策略**：对数计分意味着弱项提升收益大、接近最优时每微秒都值钱；由于瓶颈在 kernel 执行（而非 launch），优化优先级是 **kernel 内部指令级优化（向量/同步/快速数学/UB布局） > 优化最弱测试点 > 精简 TilingKey/TilingData/OpDef**。每 0.1μs 的 kernel 优化都直接反映在 OJ 得分上。
>
> **关键分水岭**：t/T = 1.5（慢 50%）时得分腰斩到 50；t/T > 2 后得分下降明显放缓。把任何一个测试点从 t/T>2 降到 <1.5，都是高收益操作。