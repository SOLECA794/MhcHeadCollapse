---
name: 可以-按优先级来-知道能打通就行了
description: 可以，按优先级来 知道能打通就行了 — verified by 4 checks
triggers: ['mhc_head_collapse', 'null', 'STATUS', '按优先级来']
---

# 可以-按优先级来-知道能打通就行了

> 自动从会话 83642d4c 蒸馏的草稿。审核后用 `/skill approve 可以-按优先级来-知道能打通就行了` 入库，或 `/skill reject 可以-按优先级来-知道能打通就行了` 丢弃。

## Steps
1. 操作：todo
2. 阅读 / 搜索：/workspace/notebook13/MhcHeadCollapse/.rivet/scratch/v117_stage/code/op_host/mhc_head_collapse.cpp、wc -l /workspace/notebook13/MhcHeadCollapse/.rivet、/workspace/notebook13/MhcHeadCollapse/.rivet/scratch/v117_stage/code/op_kernel/mhc_head_collapse.cpp
3. 验证：export ASCEND_KG_API_KEY="kg-test-1489df447809de55
4. 阅读 / 搜索：python3 -c "
import json
d = json.load(open('/work
5. 验证：export ASCEND_KG_API_KEY="kg-test-1489df447809de55
6. 阅读 / 搜索：# node_id 被 head 截断了。取完整 id 再查
python3 - << 'EOF'、python3 - << 'EOF'
# n=8 fp16 逐行路径（group_=false，nH、/workspace/notebook13/MhcHeadCollapse/.rivet/scratch/v117_stage/code/op_kernel/mhc_head_collapse.cpp
7. 修改：/workspace/notebook13/MhcHeadCollapse/.rivet/scratch/v117_stage/code/op_kernel/mhc_head_collapse.cpp
8. 阅读 / 搜索：cd /workspace/notebook13/MhcHeadCollapse/.rivet/sc、S=/workspace/notebook13/MhcHeadCollapse/.rivet/scr、# 算术检查：n=8, nH=8192, fp16
python3 -c "
def align(b
9. 修改：/workspace/notebook13/MhcHeadCollapse/.rivet/scratch/v117_stage/code/op_kernel/mhc_head_collapse.cpp
10. 阅读 / 搜索：cd /workspace/notebook13/MhcHeadCollapse/.rivet/sc、S=/workspace/notebook13/MhcHeadCollapse/.rivet/scr
11. 操作：todo
12. 阅读 / 搜索：cd /workspace/notebook13/MhcHeadCollapse
cat >> FA
13. 修改：/workspace/notebook13/MhcHeadCollapse/STATUS.md
14. 阅读 / 搜索：cd /workspace/notebook13/MhcHeadCollapse
git add F

## Verified by
- export ASCEND_KG_API_KEY="kg-test-1489df447809de55e30251bf59bf2d6c"
curl -s --compressed --max-time 30 -X POST https://ascend.wiki/search \
  -H "Content-Type: application/json" \
  -H "X-API-Key: $AS (passed 0)
- export ASCEND_KG_API_KEY="kg-test-1489df447809de55e30251bf59bf2d6c"
curl -s --compressed --max-time 30 -X POST https://ascend.wiki/source \
  -H "Content-Type: application/json" \
  -H "X-API-Key: $AS (passed 0)
- export ASCEND_KG_API_KEY="kg-test-1489df447809de55e30251bf59bf2d6c"
curl -s --compressed --max-time 30 -X POST https://ascend.wiki/source \
  -H "Content-Type: application/json" \
  -H "X-API-Key: $AS (passed 0)
- # node_id 被 head 截断了。取完整 id 再查
python3 - << 'EOF'
import json, urllib.request
d = json.load(open('/workspace/notebook13/MhcHeadCollapse/.rivet/scratch/kg-ub.json'))
ids = [r.get('id','') for r in d.ge (passed 0)

<!-- skill-draft-key: 2d98ac5af8b0 -->
<!-- source-session: 83642d4c -->
