---
name: 请按这个开源仓接入昇腾图谱https-gitcode-com-agent0-kg-tools
description: 请按这个开源仓接入昇腾图谱https://gitcode.com/agent0/kg-tools — verified by 5 checks
triggers: ['null)', '请按这个开源仓接入昇腾图谱https', 'agent0', 'kg-tools']
---

# 请按这个开源仓接入昇腾图谱https-gitcode-com-agent0-kg-tools

> 自动从会话 4f88bf50 蒸馏的草稿。审核后用 `/skill approve 请按这个开源仓接入昇腾图谱https-gitcode-com-agent0-kg-tools` 入库，或 `/skill reject 请按这个开源仓接入昇腾图谱https-gitcode-com-agent0-kg-tools` 丢弃。

## Steps
1. 操作：web_fetch
2. 阅读 / 搜索：cd "d:/Desktop/OP-Learning" || exit 1
# 放到 Ascend、cd "d:/Desktop/OP-Learning/Ascend/kg-tools" || exi
3. 操作：skill
4. 阅读 / 搜索：cd "d:/Desktop/OP-Learning" || exit 1
mkdir -p .ri、D:\Desktop\OP-Learning\Ascend\kg-tools\skills\ascend-kg\SKILL.md、cd "d:/Desktop/OP-Learning/Ascend/kg-tools" || exi
5. 验证：KEY="kg-test-1489df447809de55e30251bf59bf2d6c"
ech、KEY="kg-test-1489df447809de55e30251bf59bf2d6c"
cd
6. 阅读 / 搜索：cd "d:/Desktop/OP-Learning" || exit 1
echo "=== 1.、cd "d:/Desktop/OP-Learning" || exit 1
ls .rivet/sk

## Verified by
- cd "d:/Desktop/OP-Learning/Ascend/kg-tools" || exit 1
echo "=== 环境变量是否已有 Key ==="
echo "ASCEND_KG_API_KEY=${ASCEND_KG_API_KEY:-<未设置>}"
echo ""
echo "=== /health 预检(测试 Key) ==="
curl -sf --max-time 8 - (passed 0)
- KEY="kg-test-1489df447809de55e30251bf59bf2d6c"
echo "=== golden query 自检 ==="
curl -s --max-time 15 -X POST https://ascend.wiki/search -H "Content-Type: application/json" -H "X-API-Key: $KEY" -d '{"qu (passed 0)
- KEY="kg-test-1489df447809de55e30251bf59bf2d6c"
cd "d:/Desktop/OP-Learning"
curl -s --max-time 15 -X POST https://ascend.wiki/search -H "Content-Type: application/json" -H "X-API-Key: $KEY" -d '{"query (passed 0)
- KEY="kg-test-1489df447809de55e30251bf59bf2d6c"
cd "d:/Desktop/OP-Learning/.rivet/scratch" || exit 1
mkdir -p kg-guides && cd kg-guides
for node in kg-agent-orientation kg-skill-abtest-final kg-query-g (passed 0)
- KEY="kg-test-1489df447809de55e30251bf59bf2d6c"
cd "d:/Desktop/OP-Learning"
echo "=== 验证查询1: ReduceSum fp16(对应 F2.7 的坑) ==="
curl -s --compressed --max-time 20 -X POST https://ascend.wiki/search -H "Co (passed 0)

<!-- skill-draft-key: 3ebdb47c0b45 -->
<!-- source-session: 4f88bf50 -->
