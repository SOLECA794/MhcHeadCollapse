---
name: 按这个开源仓接入昇腾图谱https-gitcode-com-agent0-kg-tools
description: 按这个开源仓接入昇腾图谱https://gitcode.com/agent0/kg-tools — verified by 3 checks
triggers: ['ENVIRONMENT', '按这个开源仓接入昇腾图谱https', 'agent0', 'kg-tools']
---

# 按这个开源仓接入昇腾图谱https-gitcode-com-agent0-kg-tools

> 自动从会话 83642d4c 蒸馏的草稿。审核后用 `/skill approve 按这个开源仓接入昇腾图谱https-gitcode-com-agent0-kg-tools` 入库，或 `/skill reject 按这个开源仓接入昇腾图谱https-gitcode-com-agent0-kg-tools` 丢弃。

## Steps
1. 操作：web_fetch
2. 阅读 / 搜索：ls /workspace/notebook13/MhcHeadCollapse/.rivet/sk、env | grep -i "ASCEND_KG" || echo "no ASCEND_KG_AP
3. 验证：export ASCEND_KG_API_KEY="kg-test-1489df447809de55
4. 阅读 / 搜索：grep -n -A 15 "API 速查\|api/query\|curl" /workspace、sed -n '148,190p' /workspace/notebook13/MhcHeadCol、sed -n '150,162p' /workspace/notebook13/MhcHeadCol
5. 修改：/workspace/notebook13/MhcHeadCollapse/.rivet/ENVIRONMENT.md
6. 阅读 / 搜索：grep -n "^## " /workspace/notebook13/MhcHeadCollap
7. 修改：/workspace/notebook13/MhcHeadCollapse/.rivet/ENVIRONMENT.md

## Verified by
- export ASCEND_KG_API_KEY="kg-test-1489df447809de55e30251bf59bf2d6c"
curl -s -m 15 "https://ascend.wiki/api/query?query=ReduceSum%20float16%20count%20参数&key=$ASCEND_KG_API_KEY" -o /workspace/notebook13 (passed 0)
- curl -s --compressed --max-time 30 -X POST https://ascend.wiki/search \
  -H "Content-Type: application/json" \
  -H "X-API-Key: kg-test-1489df447809de55e30251bf59bf2d6c" \
  -d '{"query": "aclnnGetWo (passed 0)
- # 写入 ~/.bashrc 让所有会话可用（若已存在则跳过）
grep -q "ASCEND_KG_API_KEY" ~/.bashrc 2>/dev/null || echo 'export ASCEND_KG_API_KEY="kg-test-1489df447809de55e30251bf59bf2d6c"' >> ~/.bashrc
tail -2 ~/.bashrc (passed 0)

<!-- skill-draft-key: f0eea95dbb05 -->
<!-- source-session: 83642d4c -->
