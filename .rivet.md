# Project

<!-- 天枢 reads this file on every session start. Keep it short. -->

## Stack
- Language: C++ (AscendC / CANN kernel)
- Build: CMake → custom_opp_aarch64 (算子包)，详见 `./code/CMakeLists.txt`
- Test: 本地 NPU 硬件测试（`/dev/davinci2`，`/usr/local/Ascend/driver/`）+ OJ 提交验证（cannjudge.cn）

## Environment
- **NPU 设备**: `/dev/davinci2`（物理 NPU，非远程）
- **CANN 驱动**: `/usr/local/Ascend/driver/`（`npu-smi` CLI 在 `/usr/local/bin/npu-smi`，当前 CLI 无输出，驱动在位）
- **代码目录**: `./code/`（含 `op_host/` + `op_kernel/` 双 CMake 子项目）
- **OJ 平台**: cannjudge.cn（中国电信星辰杯 C 组 MhcHeadCollapse 算子性能赛）

## Conventions
- 遵循 `./AGENTS.md` 通用行为纪律
- 每会话开始读 `.rivet/HANDOFF.md` 获取上下文与卡点，`STATUS.md` 看当前活跃工作
- **会话结束必更新 `STATUS.md`**（滚动状态板：完成/进行中/阻塞/下一步）
- **NPU/CANN 环境事实见 `.rivet/ENVIRONMENT.md`**（CANN 8.5 在 /tmp/cann85，9.0 在 /opt/conda/Ascend；跑 NPU 命令前必读，361001 根因在内）
- 跨会话记忆：`.rivet/knowledge/`（memory-index.sqlite）
- 临时探针/脚本放 `.rivet/scratch/`
- 新事实追加到 `FACTS.md`（§N 起为本环境实测），新坑追加到 `PITFALLS.md`（P20 起）
